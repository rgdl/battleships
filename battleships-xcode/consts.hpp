#ifndef CONSTS_H
#define CONSTS_H

/*
 * If a HIDDEN_SHIP is selected, it becomes HIT.
 * If an EMPTY is selected, it becomes MISS.
 */

enum CELL_STATES {HIDDEN_SHIP, EMPTY, HIT, MISS, SUNK_SHIP};

#endif
